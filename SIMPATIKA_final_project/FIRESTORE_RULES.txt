// Example Firestore rules - adapt before production
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /jenis_surat/{doc} { allow read: if true; allow write: if request.auth != null; }
    match /pengajuan_surat/{doc} { allow read, write: if request.auth != null; }
    match /pagade_produk/{doc} { allow read: if true; allow write: if request.auth != null; }
  }
}
